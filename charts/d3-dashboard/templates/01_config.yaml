{{- if .Values.envVars }}
{{- with .Values }}

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ index . "appName" }}-config
data:
  {{- range $key, $value := .envVars }}
    {{ $key }}: {{ $value }}
  {{- end }}

{{- end }}
{{- end }}
